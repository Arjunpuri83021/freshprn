ps aux | grep node
echo "Checking if Next.js is running on port 3000"
netstat -tlnp | grep :3000
echo "Checking API endpoint"
curl -s http://localhost:3000/getnewVideos | head -20
echo "Checking env variables in the app directory"
cd /root/next-hexmy
ls -la
cat .env* 2>/dev/null || echo "No .env files"
echo "Checking package.json"
cat package.json | grep -A 10 scripts
echo "Checking if pm2 or other process manager"
ps aux | grep pm2
echo "Checking nginx status"
systemctl status nginx --no-pager
echo "Checking logs"
tail -20 /var/log/nginx/error.log
tail -20 /var/log/nginx/access.log
